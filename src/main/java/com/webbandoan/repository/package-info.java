/**
 * Layer Repository: giao tiếp với database (CRUD, truy vấn).
 * Interface kế thừa JpaRepository<Entity, Id> hoặc thêm method custom.
 * Các interface: FoodRepository, CategoryRepository, UserRepository, OrderRepository, ...
 */

package com.webbandoan.repository;

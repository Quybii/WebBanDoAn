/**
 * Layer Entity: class ánh xạ 1-1 với bảng trong database (JPA).
 * Dùng annotation @Entity, @Table, @Id, @ManyToOne, @OneToMany, ...
 * Các class: User, Role, Category, Food, Order, OrderDetail, CartItem.
 */

package com.webbandoan.entity;
